apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clamav-notify
  annotations:
    volume.beta.kubernetes.io/storage-class: gp2-encrypted
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi