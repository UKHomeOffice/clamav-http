apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clamav.fullname" . }}-test-connection"
  labels:
    {{- include "clamav.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clamav.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
